mr
=====

An OTP application

Build
-----

    $ rebar3 compile


rebar3 release

sudo bash ./ubuntu_copy.sh

sudo riak attach

code:add_pathz("/usr/lib/riak/lib/go-0.1.0/ebin").


application:start(go).

application:stop(go).
